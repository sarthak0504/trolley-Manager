{
  "name": "trolley-monorepo",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "scripts": {
    "client": "npm run start --workspace=client",
    "server": "npm run dev --workspace=server",
    "install:all": "npm install"
  }
}
